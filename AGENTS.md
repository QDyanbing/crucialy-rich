# AGENTS.md

- 新建分支要求：不带 `codex` 前缀，不带 `/`，使用 `aaa-bbb-ccc` 这种格式。
- 创建 PR 时：PR 标题使用英文，PR 内容使用中文。
- 当前仓库阶段：先搭建富文本组件工程骨架，除非用户明确要求，不实现编辑器内核或 React 组件逻辑。
- 默认使用 `pnpm` 管理依赖，遵循仓库内的 Node.js、pnpm 和 Volta 版本限制。
- 提交信息遵循 Conventional Commits，具体规则以仓库内 commitlint 配置和文档为准。

